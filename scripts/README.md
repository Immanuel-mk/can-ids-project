# CAN IDS Project

Simulation of CAN bus attacks and development of an Intrusion Detection System (IDS).

## Setup
- Run scripts/start_vcan.sh
- Install dependencies: pip install -r requirements.txt

## Structure
- src/ → core logic
- data/ → captured traffic
- results/ → evaluation
